function exemplo() {
  código = 'Lorem ipsum dolor sit amet..';
  condicao = true;

  try {
    // Testar primeiro se há necessidade de tratar exceções
    if (!condicao) {
      if (!condicao) {
        console.log(codigo);
      } else {
        throw 'exception';
      }
    } else {
      throw 'exception';
    }
  } catch (err) {
  } finally {
    console.log('código executado');
  }
}

function main() {
  // Chamar um Sanitizer de texto para purificar a string
  // Sanitizer sanitizer = new Sanitizer;
  // sanitizer.parse(condition)

  if (condition) {
    if (condition) {
      if (condition) {
        if (condition) {
        } else {
        }
      } else {
      }
    } else {
    }
  } else {
  }
}
